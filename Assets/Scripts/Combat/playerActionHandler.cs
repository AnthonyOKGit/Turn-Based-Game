using UnityEngine;

public class playerActionHandler : MonoBehaviour
{
    // Actions are scriptable objects that hold data for different types of actions
    // playerData holds an inventory of actions that the player can use (Stored as a list of scriptable objects)
    // This script is what handles actions being used in combat

    public void PlayerUseAction(action actionToUse, enemyData targetEnemy)
    {
        
    }
        
}
